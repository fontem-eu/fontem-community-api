fastapi>=0.115
uvicorn[standard]>=0.34
# python-jose 3.3 had CVE-2024-33663 (JWT alg-confusion → auth bypass,
# CVSS 9.3) and CVE-2024-33664 (DoS via crafted token). Fixed in 3.4.0.
python-jose[cryptography]>=3.4.0
# pydantic 2.0 had several follow-on CVEs in pydantic-core; 2.4 is the
# safer floor.
pydantic[email]>=2.4
loguru>=0.7
httpx>=0.27
pytest>=9.0.3
pytest-asyncio>=0.24
# starlette <0.49 had CVE-2025-54121 (multipart DoS) + CVE-2025-62727.
starlette>=0.49.1
sqlalchemy[asyncio]>=2.0
asyncpg>=0.30
alembic>=1.14
bcrypt>=4.0
nh3>=0.2
dishka>=1.3
testcontainers[postgres,minio]>=4.0
psycopg2-binary>=2.9
minio>=7.2
# python-multipart 0.0.9 had multiple DoS + arbitrary-file-write CVEs.
# 0.0.27 fixes the last of them (unbounded-header DoS).
python-multipart>=0.0.27
slowapi>=0.1.9

# (retrigger CI to pick up sbom-generate v2 + DT NVD/OSV DB — see void42/ci-actions#6)
