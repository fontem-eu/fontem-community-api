fastapi>=0.115
uvicorn[standard]>=0.34
# JWTs are signed HS256 (internal) and Google id_tokens verified RS256.
# Use PyJWT rather than python-jose: python-jose pulls the `ecdsa` package,
# which has an unfixable Minerva timing side-channel (PYSEC-2026-1325,
# won't-fix upstream) that we never exercise (no EC keys). PyJWT[crypto]
# gives RS256 via `cryptography` only. History: python-jose 3.3 had
# CVE-2024-33663 (alg-confusion → auth bypass,
# CVSS 9.3) and CVE-2024-33664 (DoS via crafted token). Fixed in 3.4.0.
PyJWT[crypto]>=2.13.0
# pydantic 2.0 had several follow-on CVEs in pydantic-core; 2.4 is the
# safer floor.
pydantic[email]>=2.4
loguru>=0.7
httpx>=0.27
pytest>=9.0.3
# Used by the assistant eval fixture (evals/prompts.yaml) and its tests.
# Was present only transitively, which meant the eval harness depended on
# something nothing declared.
PyYAML>=6.0
pytest-asyncio>=0.24
# starlette floor tracks OSV: <1.3.1 had a urlencoded-form DoS (limits
# ignored) + a Windows-only StaticFiles UNC SSRF (we run Linux).
starlette>=1.3.1
sqlalchemy[asyncio]>=2.0
asyncpg>=0.30
alembic>=1.14
bcrypt>=4.0
nh3>=0.2
dishka>=1.3
testcontainers[postgres,minio]>=4.0
psycopg2-binary>=2.9
minio>=7.2
# File-upload security pipeline (2026-06 DAST-driven hardening):
# - Pillow re-encodes raster images, killing EXIF / ICC payloads,
#   capping pixel dims (default ~178 Mpx DecompressionBomb check),
#   and proving the bytes are a real raster. >=10.0.1 closes
#   CVE-2023-50447 (eval in PIL.ImageMath).
# - python-magic detects the actual MIME from the leading bytes so
#   the client can't spoof Content-Type to slip a polyglot through.
# - clamd talks to the ClamAV daemon over TCP INSTREAM (deployed in
#   fontem-shared) — the final gate for known-malware signatures.
Pillow>=12.3.0
python-magic>=0.4.27
clamd>=1.0.2
# python-multipart <0.0.30 had a querystring O(n^2) DoS + earlier CVEs.
# 0.0.27 fixes the last of them (unbounded-header DoS).
python-multipart>=0.0.30
slowapi>=0.1.9

# (retrigger CI to pick up sbom-generate v2 + DT NVD/OSV DB — see void42/ci-actions#6)

# Second assistant executor, behind ASSISTANT_ENGINE=langgraph. Imported
# lazily so a deployment on the native engine never pays for it, but pinned
# here because the image has to contain it for the flag to be flippable.
langchain>=1.3.14
langchain-openai>=1.4.3
langgraph>=1.2.10

# Third assistant executor, behind ASSISTANT_ENGINE=pydantic-ai. Imported
# lazily like the LangGraph one, but pinned here because the image has to
# contain it for the flag to be flippable.
pydantic-ai>=2.27.0
